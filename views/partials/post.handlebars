<div class="col s12 m6">
    <div class="card blue-grey darken-1">
        <div class="card-content white-text">
            <span class="card-title">{{post.post_title}}</span>
            <p>{{post.post_content}}</p>
            <p>Posted by {{post.user.name}} on {{format_date date_posted}}</p>
        </div>
        <div class="card-action">
            {{!-- todo: add if logic to this section for buttons only users should be able to update and delete, any should be able to reply. Use session:user_ID for identification
            also add if statement for if logged in to wrap the other if statement. --}}
            <a class="waves-effect waves-teal btn-flat" id="deleteBTN" data-post="{{post.id}}" data-user="{{post.user_id}}">Delete</a>
            <a class="waves-effect waves-teal btn-flat" id="updateBTN" data-post="{{post.id}}" data-user="{{post.user_id}}">Update</a>
            <a class="waves-effect waves-teal btn-flat" id="replyBTN" data-post="{{post.id}}">Reply</a>
        </div>
    </div>
</div>
